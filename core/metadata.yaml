identity:
  group_id: group_1
  node_id: lenovo_node
  description: test
  ip: 192.168.0.238

services:
  device_services:
  application_services:
    - service: MQTT
      description: MQTT publisher
      config: applications/MQTT/mqtt_publisher.yaml
      enabled: true
